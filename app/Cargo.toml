[package]
name = "rutot"
edition.workspace = true
version.workspace = true

[dependencies]
rutot-core = { path = "../core" }
bevy = { version = "0.19", default-features = false, features = ["2d", "3d", "ui", "default_font", "area_light_luts"] }

[features]
dev = ["bevy/dynamic_linking"]
